# Browser calls this origin + /auth, /clients, …
# Local dev: Vite dev server → full URL to API
# Docker / nginx: use /api (same origin, proxy to backend)
VITE_API_BASE_URL=http://localhost:3000/api

# Optional: Bot username for deep links (without @)
# VITE_TELEGRAM_BOT_NAME=your_bot_name
